kind: task
name: simple-task-example

dictionary:
  date: "$DATE"
  token: simple-token

triggers:
  - type: timer
    options:
      period: 3

inputs:
  - app: simple
    mapper:
      content:
        type: variable
        value: "content"

  - app: simple
    mapper:
      content:
        type: variable
        value: "content"

    conditions:
      - operator: eq
        values:
          - "$.content"
          - "apple"

outputs:
  - app: print
    options:
      content: "$.content"
  - app: print
    options:
      content: "$.token"
  - app: print
    options:
      content: "$.date"
  - app: print
    options:
      content: "$time"
